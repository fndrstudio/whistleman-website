---
phase: 01-critical-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/vendor/
  - assets/vendor copy/
  - index.html
  - services.html
  - portfolio.html
  - contact.html
  - portfolio/burgernshake.html
  - portfolio/inner circle.html
  - portfolio/l'OUI.html
  - portfolio/linguini.html
  - portfolio/locals.html
  - portfolio/oeuf.html
  - portfolio/pazzi.html
autonomous: true

must_haves:
  truths:
    - "Site loads without broken asset references"
    - "No 'vendor copy' paths exist in any HTML file"
    - "Total assets size reduced by ~10MB"
  artifacts:
    - path: "assets/vendor/"
      provides: "Single consolidated vendor directory"
      contains: "bootstrap, bootstrap-icons, glightbox, php-email-form, purecounter, swiper"
    - path: "index.html"
      provides: "Homepage with correct vendor paths"
      contains: 'href="assets/vendor/'
  key_links:
    - from: "*.html"
      to: "assets/vendor/"
      via: "link href and script src attributes"
      pattern: 'assets/vendor/[^"]+\.(css|js)'
---

<objective>
Remove vendor directory bloat and fix folder naming.

Purpose: Eliminate 10MB of duplicate files and fix the awkward "vendor copy" naming that causes path confusion.
Output: Clean vendor directory structure with consistent HTML references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md

# Current state verified:
# - assets/vendor/ (9.9MB) - UNUSED duplicate, 0 HTML references
# - assets/vendor copy/ (10MB) - ACTIVE, all 11 HTML files reference it
# - AOS: loaded but never used (no data-aos attributes, no JS init)
# - Isotope/ImagesLoaded: script tags exist but never used
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate vendor directories</name>
  <files>assets/vendor/, assets/vendor copy/</files>
  <action>
    1. Delete the unused duplicate: `rm -rf assets/vendor/`
    2. Rename the active directory: `mv "assets/vendor copy" assets/vendor`

    The space in "vendor copy" causes issues. After rename, all paths become cleaner.

    Do NOT modify any HTML files in this task - that's Task 2.
  </action>
  <verify>
    - `ls assets/` shows only: css, img, js, scss, vendor (no "vendor copy")
    - `ls assets/vendor/` shows: aos, bootstrap, bootstrap-icons, glightbox, imagesloaded, isotope-layout, php-email-form, purecounter, swiper
  </verify>
  <done>Single vendor directory exists at assets/vendor/, no duplicate</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML vendor paths</name>
  <files>index.html, services.html, portfolio.html, contact.html, portfolio/burgernshake.html, portfolio/inner circle.html, portfolio/l'OUI.html, portfolio/linguini.html, portfolio/locals.html, portfolio/oeuf.html, portfolio/pazzi.html</files>
  <action>
    Find and replace in all 11 HTML files:
    - Change: `assets/vendor copy/` → `assets/vendor/`

    This affects all link href and script src attributes referencing vendor files.
    Use sed or direct file editing. Example pattern:
    `sed -i '' 's|assets/vendor copy/|assets/vendor/|g' *.html portfolio/*.html`

    Be careful with files that have spaces in names (portfolio/inner circle.html, portfolio/l'OUI.html).
  </action>
  <verify>
    - `grep -r "vendor copy" *.html portfolio/*.html` returns NO results
    - `grep -c "assets/vendor/" index.html` returns count > 0 (paths updated)
    - Open index.html in browser - no 404 errors in console for vendor files
  </verify>
  <done>All 11 HTML files reference "assets/vendor/" not "vendor copy"</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused libraries</name>
  <files>assets/vendor/aos/, assets/vendor/isotope-layout/, assets/vendor/imagesloaded/, all HTML files</files>
  <action>
    1. Delete unused library directories:
       - `rm -rf assets/vendor/aos/`
       - `rm -rf assets/vendor/isotope-layout/`
       - `rm -rf assets/vendor/imagesloaded/`

    2. Remove references from HTML files (all 11 files):
       - Remove: `<link href="assets/vendor/aos/aos.css" rel="stylesheet">`
       - Remove: `<script src="assets/vendor/aos/aos.js"></script>`
       - Remove: `<script src="assets/vendor/imagesloaded/imagesloaded.pkgd.min.js"></script>`
       - Remove: `<script src="assets/vendor/isotope-layout/isotope.pkgd.min.js"></script>`

    These libraries are loaded but never used - verified:
    - AOS: No data-aos attributes in HTML, no AOS.init() in main.js
    - Isotope/ImagesLoaded: Script tags exist but no usage in code

    Do NOT remove: bootstrap, bootstrap-icons, glightbox, php-email-form, purecounter, swiper (these ARE used)
  </action>
  <verify>
    - `ls assets/vendor/` shows 6 directories: bootstrap, bootstrap-icons, glightbox, php-email-form, purecounter, swiper
    - `grep -r "aos\|isotope\|imagesloaded" *.html portfolio/*.html` returns NO results
    - Open index.html in browser - page loads correctly, no missing functionality
  </verify>
  <done>Unused libraries deleted from vendor and all HTML references removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ls assets/` shows vendor directory (not "vendor copy")
- [ ] `grep -r "vendor copy" *.html portfolio/*.html` returns nothing
- [ ] `grep -r "aos\|isotope\|imagesloaded" *.html portfolio/*.html` returns nothing
- [ ] `du -sh assets/vendor/` shows ~6MB (down from 10MB)
- [ ] Open index.html locally - loads without console errors
- [ ] Open portfolio/burgernshake.html locally - loads without console errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Site loads correctly without broken vendor references
- Total size reduced by ~14MB (10MB duplicate + 4MB unused libs)
- Clean "assets/vendor/" path in all HTML files
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-cleanup/01-01-SUMMARY.md`
</output>
